import math
length = input("Please input a length: ")
length = float(length)
area = math.pow(length, 2)
print("The area of a square of side length", length, "is:", area)

r= input("Enter the radius for the a Norman Window shape: ")
